# Torch runtime (universal fallback; Linux/CUDA servers).
laya>=0.1.0
# Native MLX runtime for Apple Silicon — preferred backend on macOS.
# Skipped automatically on other platforms / older Python.
laya-mlx>=0.1.0; sys_platform == "darwin" and python_version >= "3.11"
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0
